'use strict';

/**
 * integrante-tienda service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::integrante-tienda.integrante-tienda');
